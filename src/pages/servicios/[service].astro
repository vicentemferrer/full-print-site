---
import Layout from '@layouts/Layout.astro';
import ServiceGallery from '@components/GalleryComponents/ServiceGallery.jsx';

export async function getStaticPaths() {
  return [
    {
      params: { service: 'documentos' },
      props: { subtitle: 'Impresión de Documentos', categorySign: 'docs' }
    },
    {
      params: { service: 'imagenes' },
      props: { subtitle: 'Impresión de Imágenes', categorySign: 'pict' }
    },
    {
      params: { service: 'publicidad' },
      props: { subtitle: 'Herramientas de Publicidad', categorySign: 'mktg' }
    },
    {
      params: { service: 'diseno' },
      props: { subtitle: 'Diseño Gráfico', categorySign: 'dsgn' }
    },
    {
      params: { service: 'otros' },
      props: { subtitle: 'Otros Servicios', categorySign: 'misc' }
    }
  ];
}

const { subtitle, categorySign } = Astro.props;
---

<Layout {subtitle}>
  <main data-category={categorySign}>
    <h2>{subtitle}</h2>
    <ServiceGallery client:load />
  </main>
</Layout>

<style>
  main h2 {
    grid-column: 2 / 3;

    text-align: center;
    margin-block: 2rem;

    font-size: xx-large;
  }
</style>